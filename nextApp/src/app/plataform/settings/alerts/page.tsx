import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Alerts & Notifications",
};

export default function AlertsSettingsPage() {
  return (
    <div>
      <h1 className="font-bold text-2xl">Alert Settings</h1>
      Not implemented
    </div>
  );
}
